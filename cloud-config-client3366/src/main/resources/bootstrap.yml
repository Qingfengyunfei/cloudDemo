server:
  port: 3366
spring:
  application:
    name: config-client-plus
  cloud:
    config:
      label: master #分支名称
      name: config #配置文件名称
      profile: company #读取后缀名称  上述3者综合：master分支上config-company.yml
      uri: http://localhost:3344 #配置中心地址
  #rabbitmq相关配置
  rabbitmq:
    host: 192.168.79.101
    port: 5672
    username: admin
    password: jing16
eureka:
  client:
    service-url:
      defaultZone: http://eureka9001.com:9001/eureka,http://eureka9002.com:9002/eureka #集群版

management:
  endpoints:
    web:
      exposure:
        include: "*"